const loader = document.getElementById('loader');

startPage = () => {
    page.classList.remove("hidden"); //the loader appears until the page starts
    loader.classList.ass("hidden"); //the loader dissapears
}
